#version 330 core
layout (location = 0) in vec3 aPos;
out vec2 VertPos;
uniform vec4 Zoom;

void main()
{
    gl_Position = vec4(aPos.x, aPos.y, aPos.z, 1.0);
    VertPos = vec2(Zoom.x + aPos.x * Zoom.z, Zoom.y + aPos.y * Zoom.w);
}
